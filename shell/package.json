{
  "name": "ssid-shell",
  "version": "0.1.0",
  "private": true,
  "description": "SSiD 壳（思灵）：Electron 补丁层，boot 隔壁 DSH 官方 UI 并外挂侧栏",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "node --import tsx/esm boot-smoke.ts"
  },
  "main": "main.mjs",
  "dependencies": {
    "electron": "43.3.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^26.0.0"
  }
}
