# v0.1.1 补丁

## 修复

- **EPIPE 崩溃**：主进程 stderr 管道断开（如启动终端关闭）后继续写日志会抛
  `EPIPE: broken pipe`，升级为未捕获异常并弹出「A JavaScript error occurred」。
  现在全局防护 + 写失败静默，任何启动方式下不再弹错误框、不再崩溃。

## 更新说明

- 已安装 v0.1.0 的用户：设置 →「关于 SSiD」→「立即检查」会显示 v0.1.1 可用，
  手动下载本安装包覆盖安装即可。
- v0.1.0 Release 保留，可回退。
