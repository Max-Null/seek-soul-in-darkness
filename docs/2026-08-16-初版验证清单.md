# SSiD 初版验证清单（醒来照做）

> 日期：2026-08-16
> 状态：资产已部署，待人工验证
> 部署位置：`C:\Users\MaxNull\.dsh\`（你的 DSH home）

## 一、已部署的资产

| 资产 | 部署到 | 数量 |
|---|---|---|
| 技能 | `~/.dsh/skills/` | 14 个（8 mxy + 6 omo） |
| 主 agent preset | `~/.dsh/.agent-presets/ssid-double-star/` | 1 个（双星） |
| 插件 | 通过 `dsh plugin add`（dsh-memory / dsh-chinese-thinking） | 2 个 |

## 二、已生效的证据（无需你动手，已自动发生）

- ✅ **14 个技能已被 DSH 加载**——本会话的 skill 目录已实时出现 `mxy-*`（commit-review/design-doc/git-pull/organize-code/organize-scss/pptx-slim/update-docs/upgrade-vue3）和 `omo-*`（clonedeps/codemap/reflect/simplify/verification-planning/worktrees）。这是 skill-filesystem 的 watch 机制自动发现的，**没重启就生效了**。

## 三、醒来后你要验的（约 10 分钟）

1. **技能**：随便说一句「用 mxy-commit-review 帮我审查」或「整理一下这个代码」，看模型能不能调起技能（skill 工具会加载对应 SKILL.md）。

2. **双星 preset**：新建一个会话，看 agent preset 选择器里有没有「双星」；选它，看会话开头是不是"双星"的人设（先对齐再动手）。

3. **中文思考**：说句话，看是不是首轮就中文思考（chinese-thinking 插件）。

4. **记忆**：让模型调一下 `memory_save`，看记忆工具是否可用。

## 四、预期 vs 待办

| 项 | 预期 |
|---|---|
| 技能 | ✅ 应该已可用（catalog 已出现） |
| preset | ⚠️ 已部署，需新建会话选「双星」验证（当前会话的 agent 不会自动切换） |
| 插件 | ✅ dsh-memory / dsh-chinese-thinking 已在 web profile 生效 |
| 记忆 UI 面板 | ⏳ 未完成（Typert 产物需宿主侧，见设计文档） |

## 五、验证时可能遇到

- preset 选择器里看不到「双星」→ 可能 dsh web 需要重启（preset discovery 是每次读的，理论上不需要，但若缓存了旧 roster 则重启）。
- 技能调不起来 → 报错发我，我排查 SKILL.md 格式。
