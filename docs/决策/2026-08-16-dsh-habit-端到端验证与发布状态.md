# dsh-habit 端到端验证与发布状态

- 日期：2026-08-16
- 状态：端到端验证通过；npm 发布被浏览器认证阻塞（待用户操作）
- 验证脚本：`shell/_eval-habit.ts`
- 相关设计：`docs/设计/2026-08-16-自学习闭环-设计.md`

## 验证结果

在 SSiD 壳内直接 `boot()` DSH，通过 TEMP `file://` 引用加载 dsh-habit
（`~/.dsh/profiles/ssid/cordis.patch.yml`），完整走通自学习三段闭环：

1. **观察**：注入 3 条纠错信号（turn 内 `isCorrection` 文本），`turn/end` 触发串行分析
2. **判断**：flash（temperature 0 + 严格 JSON）生成 1 条候选：
   `[pending] high :: 用户每次提交前要求先跑一遍测试再交付 (证据 3 条)`
3. **沉淀**：`confirm` 后候选转 `confirmed`，写入 dsh-memory 为
   `[suggested]`，停在人工闸门（`memory_confirm` 后转 `auto`）

与 dsh-habit 单元测试（11 条，analyzer/signals/candidates/engine 全绿）、
typecheck、build 相互印证。TEMP `file://` 引用已从 profile 清理。

## 遗留阻塞（均需用户侧操作）

1. **dsh-habit npm 发布**：`npm publish` 返回浏览器认证 URL（token 在
   输出中被掩码）。用户需在终端自行执行 `npm publish` 完成浏览器认证。
   发布后：
   - `~/.dsh/profiles/ssid/cordis.patch.yml` 以 bundle 方式加回 `habit` 行
   - SSiD shell 换用正式包名（当前验证走 `file://`）
2. **明瞳（制图师）kimi-k3**：pi-ai 目录无 kimi-k3（仅 kimi-k2），且无
   `MOONSHOT_API_KEY`。配置指南已写：`docs/决策/明瞳-kimi-k3-配置指南.md`。

## 决策

- dsh-habit 验证期不保留 TEMP `file://` 引用：生产 profile 保持干净，
  发布后一步换 bundle（留尾清零原则）。
- `_eval-habit.ts` 随仓库提交：它是 habit 闭环的回归验证脚本，与
  `_eval-roles.ts` 同级留档，供发布后复跑。
