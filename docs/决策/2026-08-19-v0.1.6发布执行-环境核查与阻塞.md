# v0.1.6 发布执行：环境核查与阻塞记录

> 日期：2026-08-19（拉取代码后当轮）
> 状态：**执行中，卡在待办 1 的 npm publish**（凭据由用户手动执行；**0.1.7 代码确认不在 GitHub 远程**，publish 需用户在另一环境本地仓库执行）
> 关联：[`2026-08-19-v0.1.6发布待办-标题栏按钮组.md`](2026-08-19-v0.1.6发布待办-标题栏按钮组.md)

---

## 一、当前环境核查结果（本机 H:\MaxNull\WorkStation\seek-soul-in-darkness）

| 项 | 结果 | 影响 |
|---|---|---|
| SSiD 仓库 | 存在，main 在 `8c02389`（含 5 个发布相关 commits） | ✅ |
| `shell/` 目录 | 存在 | ✅ |
| Node | v26.2.0（PATH） | ✅ 满足 prepare-runtime ≥22.13 |
| pnpm | 11.21.0 | ✅ |
| npm registry | registry.npmjs.org，可达（`npm view` 成功） | ✅ |
| **npm 凭据** | `~/.npmrc` token `npm_KdnF9m1...` **401 Unauthorized**（`npm whoami` / curl 均失败） | ❌ 无法 `npm publish` |
| npm 已发布版本 | `@max-null/dsh-plugin-center` 最新 **0.1.5**（0.1.6/0.1.7 未发布） | ❌ 与待办文档一致 |
| plugin-center 本地 checkout | `D:\Project\dsh-plugin-center` **不存在**（开发在另一环境） | ❌ 无 0.1.7 代码 |
| plugin-center 远程 | GitHub 存在，main=`ebc3c0d`（**是否含 0.1.7 未确认**，文档记录 509d9f5/1833b58 开发时未 push） | ⚠️ 待确认 |
| GitHub 网络 | **间歇性不可用**：直连曾成功（pull 成功、ls-remote 一次成功），当前窗口全部超时（clone/ls-remote/codeload 均失败） | ⚠️ 时通时断 |
| git 代理 7897 | `clash-core-service` 进程在跑，但 **TLS connect error**（握手失败），不可用 | ❌ 需修代理或直连 |
| NVM_HOME | 指向 `D:\MN_Develop\nvm`，目录不存在（残留变量） | ⚠️ 不影响当前 node，但 prepare-runtime 默认找 `D:\Program Files\nvm\v22.22.2\node.exe` 也不存在——**需确认 prepare-runtime 的 node 发现逻辑** |

## 二、发布链依赖关系（卡点定位）

```
待办1  plugin-center push + publish  ←── 卡在 npm publish（用户手动）；⚠️ push 实际未完成（见下）
待办2  prepare-runtime.mjs           ←── 依赖待办1：pnpm 拉 @max-null/dsh-plugin-center@^0.1.6
                                            （8c02389 已确认：0.1.7 未发布时归档无法重建）
待办3  npm run pack                  ←── 依赖待办2（新归档）
待办4  发布前自测                     ←── 依赖待办3
待办5  GitHub Release + tag          ←── 依赖待办3 + 网络
待办6  收尾                           ←── 依赖待办5
```

## 三、阻塞点（需用户侧输入）

1. **npm 凭据**：`~/.npmrc` 的 token 已失效（401）。**用户已选择手动执行**（见第五节指令）。
2. **plugin-center 0.1.7 代码**：❌ **确认不在 GitHub 远程**——`ls-remote --heads` 只有 main（`ebc3c0d`，package.json=0.1.5），无其他分支；clone 提交历史无 509d9f5/1833b58。用户先前以为已 push，实际未成功。**publish 不依赖 push**（npm registry 与 GitHub 无关），用户在另一环境本地仓库 publish 即可；push 需用户补做（收尾待办）。
3. **GitHub 网络**：直连间歇不通（曾 11 次重试成功一次 clone）、代理 7897 已坏。影响 clone/Release 等步骤。

## 四、环境内可先行事项（不依赖网络/凭据）

- [x] 核对 prepare-runtime.mjs 的 node 发现逻辑（**只认 `DSH_NODE` 环境变量或 `D:\Program Files\nvm\v22.22.2\node.exe`**，本机两者都没有 → 需设置 `DSH_NODE` 指向 PATH 的 v26.2.0；pnpm 同理需 `PNPM_CMD` 指向 pnpm.cjs）
- [x] 预写 `docs/release-notes-v0.1.6.md`（待办 5 前置，已起草完成）
- [x] 检查 shell 打包链路本地可用性（electron 43.3.0 / electron-builder / esbuild 已装；`dsh-runtime.tar.gz` 旧归档 171MB 存在待重建）

### 待办 2 本机运行参数（已确认）

```
DSH_NODE = C:\Program Files\nodejs\node.exe      （v26.2.0，满足 ≥22.13）
PNPM_CMD = C:\Users\MaxNull\AppData\Roaming\npm\node_modules\pnpm\bin\pnpm.cjs   （11.21.0）
```

> 注意：pnpm 的 shim 是 pnpm.ps1，prepare-runtime 用 node 执行 pnpm.cjs，必须给 PNPM_CMD 指向真实 .cjs 路径。

## 五、用户手动执行部分（2026-08-19 用户选择"给我指令我去执行"）

> plugin-center 0.1.7 已 push 到 GitHub（用户确认），npm 凭据部分由用户手动执行。

### 5.1 npm 登录（二选一）

```sh
# 方式 A：交互式登录
npm login

# 方式 B：设置 token（若已有新 token）
npm config set //registry.npmjs.org/:_authToken=<你的新token>
```

验证：`npm whoami` 应输出你的 npm 用户名。

### 5.2 发布 plugin-center 0.1.7（在 plugin-center 仓库目录）

```sh
cd <plugin-center 仓库目录>      # 确认在 main 分支且包含 1833b58（0.1.7）
npm publish                       # 发布 @max-null/dsh-plugin-center@0.1.7
npm view @max-null/dsh-plugin-center version   # 应输出 0.1.7
```

> 确认仓库已含 0.1.7：`git log --oneline -3` 应看到 `1833b58`（0.1.7 toggle/close/遮罩）与 `509d9f5`（0.1.6 open）。

### 5.3 完成后告知

发布成功后告诉我，我继续执行：待办 2（prepare-runtime，需 `DSH_NODE`/`PNPM_CMD` 环境变量）→ 待办 3（pack）→ 待办 4（自测）→ 待办 5（release-notes + Release）→ 待办 6（收尾）。
