# SSiD 升级执行记录（web → SSiD 回灌，第 2 阶段）

- 日期：2026-08-29（晚）
- 执行会话：DSH web 端 3080（官方 master 源码 0.1.2-alpha.1）
- 配套文档：`docs/决策/2026-08-29-SSiD升级执行指南.md`（执行方案）、`docs/决策/2026-08-29-DSH-master插件适配测试报告.md`（第 1 阶段 web 端测试）

## 结论速览

- **插件全家桶已按 web 端实体版本全量对齐**（27 项依赖、28 项 bundle、5 处 vendor 三端 MD5 一致）——这是「web 确认可用后回灌 SSiD」的第 2 阶段落地。
- **内核已切到 master 0.1.2-alpha.1**（dev 源码模式：思灵壳 boot 并列的 deepseek-harness checkout），smoke + Electron 壳 boot 双验证通过。
- **唯一未完成**：内置闭包归档（dsh-runtime.tar.gz）仍是 rc.2——**npm 尚未发布 @deepseek-ai/dsh@0.1.2-alpha.1**（官方 `release/dsh-0.1.2-alpha.1` 分支已合并 master #3248，发布流程已启动、registry 仍 E404）。闭包重建（路径 B）需等官方发布后执行。

## 完成清单（对照 web 端升级报告）

| 项 | web 端经验（第 1 阶段） | SSiD 本次执行 | 状态 |
|---|---|---|---|
| 内核 | master 源码 0.1.2-alpha.1 | dev 源码模式：bootKernel 回退并列 checkout（`deepseek-harness`） | ✅ smoke boot OK（node exit 0）+ 壳日志 `bootKernel ok port=58125` |
| kernel.ts 与 master API | web 无壳层，不涉及 | **healProfilesModuleFallback 旧双参签名 → async options 对象**（0.1.2-alpha.1 签名变更）；`loadProfile`/`boot`/`provideCmdline`/`webServer`/`agent-presets` 契约核对全部兼容 | ✅ typecheck 通过 |
| boot-smoke 断言 | — | master web 服务带 token 认证：裸 fetch 401 = 服务就绪；断言已更新（401 亦算 ready） | ✅ |
| dsh-session-manager | 0.2.2 → 0.4.1（npm） | vendor tgz（0.2.2）→ **0.4.1**，实体 0.4.1 | ✅ |
| dsh-sidebar-qa | 0.4.2 → 0.5.0（meta 0.5.0，依赖 master 的 remote.session） | 0.4.0 → **0.5.0**，实体 0.5.0 | ✅ |
| dsh-context | 0.36 → 0.38.1 | ^0.37.0 → **0.38.1**，实体 0.38.1 | ✅ |
| dsh-pocket | 1.14.5 → 1.16.1 → 2.1.3 | 1.14.5 → **2.1.3**，实体 2.1.3 | ✅ |
| dsh-dream-skin | 8.28.1（本地修）→ npm 8.29.0（PR #42 采纳） | ^8.28.0 → **8.29.0**，实体 8.29.0 | ✅ |
| dsh-context-doctor | 0.6.1 ❌ → 0.6.2-master（本地构建，PR #10） | vendor 整体同步 web（0.6.2-master），三处 MD5 = `C6C129…` | ✅ |
| @max-null/dsh-plugin-center | 0.2.13 → 0.2.14 | link:本地仓库 → **0.2.14（npm）**，实体 0.2.14，链接已正确换货 | ✅ |
| @max-null/dsh-skill-mcp-center | 0.4.1 → 0.4.2 | ^0.4.1 → **0.4.2** | ✅ |
| @max-null/dsh-achievements | 0.1.0 → 0.1.2 | ^0.1.0 → **0.1.2** | ✅ |
| dsh-header-unify | v0.3 → v0.4（`__SSID_SHELL__` 分支） | 已同步（本会话前完成）；壳环境隐藏插件中心/侧栏原按钮、标题栏接管、footer 隐藏 | ✅ 三处 MD5 一致 |
| dsh-ssid-panels | footer 补丁 v1-v22 撤回，干净 0.1.9 | 三处 vendor 干净 0.1.9（本会话前完成） | ✅ MD5 一致 |
| open-sea-skin | 移除（web + SSiD 预制清单） | SSiD 无 open-sea-skin（bundles 28 项不含） | ✅ 与 web 一致 |
| patchReload | web manifest `live` | ssid 补 `"patchReload": "live"`（custom profile 默认亦 live，显式对齐） | ✅ |
| 依赖声明形态 | 精确 pin（无 ^） | 全部去 ^ 精确化（21 个更新 + 21 个删除） | ✅ node 写 JSON 无 BOM |
| 内置闭包族声明 | web 无 | 删除 profile/package.json 全部 `@deepseek-ai/dsh*`/cordis 内核族（21 项）——源码模式下从 checkout 解析 | ✅ pnpm install +39/-487 |

## 关键执行细节

- **内核切换**：`node_modules/@deepseek-ai`（rc.2 闭包 196 条目）整体移出 → `.upgrade-backup/@deepseek-ai-rc2`（回滚 = 移回）；dev 裸跑（`app.isPackaged=false`）无 DSH_CHECKOUT 且锚点消失 → `resolveDshRuntime()` 命中并列源码。全程无环境变量残留（避免 pitfalls #5）。
- **实体校验**：web（基准）vs ssid（实体）逐包比对，26/27 ✓；唯一差异 `@playwright/mcp 0.0.79` 为 **SSiD 预制 MCP 独有**（web 无此功能），预期内。校验脚本：`Temp\dsh-ssid-verify-plugins.mjs`。
- **smoke boot 两次发现**：① mcp-playwright 行 `args:[null,…]` —— smoke 裸跑未设 `SSID_MCP_PW_CLI`（Electron 壳由 main.mjs 注入），属测试环境缺失，补环境变量后通过；② master web 服务 token 认证（401 = ready），boot-smoke.ts 断言已更新。
- **本次修改文件（SSiD 库）**：`shell/kernel.ts`（heal API 适配）、`shell/boot-smoke.ts`（401 就绪断言）、`shell/profile-template/vendor/dsh-context-doctor`（同步 web 0.6.2-master）。
- **profile 侧**：`~/.dsh/profiles/ssid/package.json`（升级）、`~/.dsh/profiles/ssid/vendor/dsh-context-doctor`（同步）、`~/.dsh/profiles/ssid/.upgrade-backup/@deepseek-ai-rc2`（内核闭包备份）、`package.json.pre-master-20260829-181432`（依赖声明备份）。

## 待办（按序）

1. **npm 发布 `@deepseek-ai/dsh@0.1.2-alpha.1` 后 → 路径 B**（正式发版形态）：
   - `node shell/scripts/prepare-runtime.mjs`（重建 dsh-runtime.tar.gz，自动从 DSH_CHECKOUT 读 0.1.2-alpha.1 并 pin；vendor 三处已同步，归档自动带上）
   - 按 `docs/发版流程规范.md` + ssid-release skill：归档抽查（`.runtime-version` 应为 `0.1.15-0.1.2-alpha.1-<指纹>`、kernel 版本、vendor 指纹）→ NSIS 打包 → 本机验证 → GitHub tag/Release。
   - 发版前把 `kernel.ts`/`boot-smoke.ts` 的改动 bundle 进 `kernel.bundle.mjs`（打包版走 bundle 形态）。
2. **思灵 GUI 验证（用户手动启动，web 会话不启停实例）**——核对 `docs/决策/2026-08-29-SSiD升级执行指南.md` §4 清单（壳 boot、标题栏「会话管理」、插件中心 28 bundle 无 failed、footer 无会话管理按钮、console 0 error）。
3. **回滚点**：依赖声明 `package.json.pre-master-20260829-181432` + `@deepseek-ai` 移回 + git 还原 `shell/kernel.ts`/`shell/boot-smoke.ts`（三者齐备即回 rc.2 闭包形态）。

## 本轮教训（补充）

- **web 端（3080）由 opencode 启动器管理**（`Temp\opencode\dsh-web-restart.log` 可见 wrapper）；其宿主进程会按 opencode 生命周期被轮换重启，**与思灵无关**——web 会话里的工具调用若恰逢轮换会显示「interrupted」。
- 思灵（dev）与 web 共享同一 `~/.dsh` 与源码 checkout：**验证思灵时由用户手动启动/关闭**，web 会话侧不启停任何 DSH 实例（避免共享路径写入与实例干扰）。

## L1：不依赖 npm 的内核归档（2026-08-29 晚追加）

**决策**：官方 `@deepseek-ai/dsh@0.1.2-alpha.1` 未发布（所有子包 E404），但闭包不必来自 npm——本地构建 + 私有 registry 可产出同等形态。

**链路（全部成功）**：
1. 发现 deepseek-harness 工作区有 4 处本地修改：`remotes/index.ts` + `vendor/loader/tree.ts` = **诊断探针**（写 `Temp\opencode\dsh-json-diag.log`，编译不过，属进行中的调试，闭包排除）；`api-request-trust.ts` + 测试 = **web 端实测修复**（Origin 无端口同源判定，带进闭包）。
2. `git worktree add H:\MaxNull\WorkStation\dsh-build-clean HEAD`（干净官方源码，零侵入原工作区）→ 应用修复补丁（api-request-trust + spec）。
3. `pnpm install`（24s，store 复用）→ `pnpm run build` ✅（tsc + tsdown，218 client 产物）。
4. `verdaccio`（127.0.0.1:4873，本地存储 + 代理 npmjs）+ `pnpm -r publish` → **253 个 @deepseek-ai/* 包全发 0.1.2-alpha.1**（pnpm 自动把 `workspace:*` 转版本号；17 个 private（experimental/fixture）自动跳过；vendored 包按自身版本发布：cordis 4.0.1 等）。
5. `prepare-runtime.mjs` 新增 **`SSID_REGISTRY` 环境变量**（写入闭包 .npmrc；不设=官方源，发版路径不变）→ `SSID_REGISTRY=http://127.0.0.1:4873 node scripts/prepare-runtime.mjs` ✅。
6. **归档抽查（发版级）**：`.runtime-version = 0.1.15-0.1.2-alpha.1-ac40eace`；归档内 `@deepseek-ai/dsh@0.1.2-alpha.1` ✓；api-request-trust 修复（hostname 判定）已编入 dsh-client-connection lib ✓；零污染（无 .bak/诊断探针/open-sea-skin）✓；204.0 MB。
7. `kernel.bundle.mjs` 重建（kernel.ts 的 heal API 适配同步进打包形态；旧产物备份 `kernel.bundle.mjs.pre-master-20260829.bak`）。

**遗留**：
- 首次归档曾带入 `dsh-context-doctor.bak-20260829-181432`（备份目录被当 vendor 条目卷进），已清理 template/vendor 后重跑消除（指纹 87e1f579 → ac40eace）。
- **部署验证**：dev 模式默认不自动部署（devSkipDeploy）——启动时设 `SSID_DEV_DEPLOY=1` 触发版本不一致强制部署新闭包（rc.2 → 0.1.2-alpha.1）；之后恢复源码/闭包模式的切换以 `.runtime-version` 与部署日志为准。
- 官方 npm 发布后：本地 registry 与闭包可无缝切回官方源（prepare-runtime 不设 SSID_REGISTRY 即官方路径），差异仅为构建出处。
