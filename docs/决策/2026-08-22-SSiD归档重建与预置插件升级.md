# SSiD 归档重建与预置插件升级（2026-08-22，接手误关闭会话）

## 背景

前一会话正在执行 SSiD 归档发版链时被误关闭（`shell/dsh-runtime.tar.gz` 落在 0 字节 = tar 阶段中断前的残留）。该会话已完成：
- npm 发布链：chat-rail 0.3.1（tip 缩略图 + 收起态角标）、plugin-center 0.2.3（AI 推荐修复 + 0.2.1 发布时的文档复盘）
- profile-template 依赖声明 `@max-null/dsh-plugin-center` 升至 `^0.2.3`（未提交，本次并入）

## 本次接手执行

### 1. 预置插件版本对齐（shell/profile-template/package.json，commit 580a1b4）

| 包 | 声明 | npm 最新 | 处理 |
|---|---|---|---|
| @max-null/* 全家桶 9 包 | 各 ^x.y.z | 全部一致 | 无需改（chat-rail ^0.3.1 / plugin-center ^0.2.3 等） |
| dsh-better-sidebar | ^0.14.0 | 0.15.0 | 升 ^0.15.0 |
| dsh-sidebar-qa | ^0.2.0 | 0.4.0 | 升 ^0.4.0 |
| dsh-dream-skin / open-sea-skin / vendor 三件套 | — | — | 保持 |

### 2. bundles 残留修复（关键，否则新归档部署即崩溃）

`dsh.profile.bundles` 中：
- **移除 `dsh-skin`**：依赖已在 e06a10a 移除，但 bundles 残留。kernel `resolveBundleDir` 对 bundle 列表逐包 resolve，找不到直接 throw（`cannot resolve profile bundle`）→ 新归档部署后思灵启动必崩。
- **补入 `dsh-dream-skin`、`open-sea-skin`**：两包均声明 `dsh.bundle.patch`（npm view / tgz 实测确认），不进 bundles 皮肤不会加载。

### 3. 归档重建与验证链（已执行 ✅）

- `node scripts/prepare-runtime.mjs`：node 自动命中 PATH v26.2.0，pnpm 命中 %APPDATA% 全局 cjs（无需 DSH_NODE/PNPM_CMD）；pnpm install 19.6s（967 包，store 复用 906）+ vendor 兜底正常
- 归档产物 `shell/dsh-runtime.tar.gz` **163.5 MB**，`.runtime-version = 0.1.8-0.1.1-rc.2-489c8bdf`
- 归档冒烟（tar 解压 → Node UTF-8 核对实体 package.json，**勿信 pnpm view/乱码的 PowerShell 读取**）：

| 包 | 归档内实体版本 | 期望 |
|---|---|---|
| @deepseek-ai/dsh | 0.1.1-rc.2 | ✅ |
| @max-null/dsh-chat-rail | 0.3.1 | ✅（client.js 含 crl_tipImgCount / attachmentId 新代码） |
| @max-null/dsh-plugin-center | 0.2.3 | ✅ |
| @max-null/dsh-memory | 0.5.0 | ✅ |
| dsh-better-sidebar | 0.15.0 | ✅ |
| dsh-sidebar-qa | 0.4.0 | ✅ |
| dsh-dream-skin | 0.4.10 | ✅ |
| open-sea-skin | 1.2.1 | ✅（file: tarball 本地增强版） |
| vendor 三件套 | header-unify 0.1.0 / ssid-panels 0.1.3 / ssid-zh-ui 0.1.0 | ✅ |

- 安装目录归档已替换（`C:\Users\MaxNull\AppData\Local\Programs\ssid-shell\resources\dsh-runtime.tar.gz`，旧版备份 `.bak-0822`）；profile 现 `.runtime-version=0f58d550` ≠ 归档 `489c8bdf` → 思灵下次启动自动触发重部署
- 运行 profile 全程未跑 pnpm install（pitfalls 第 11 节顺序约束 ✓，open-sea-skin 手动增强版未被恢复）

### 4. v0.1.9 发版（已执行 ✅）

- **版本决策**：远端 tag v0.1.8 已锚定 8/21 rc.8 时代提交（且 v0.1.7/v0.1.8 均未建 Release），本次 rc.2 内容不可复用 0.1.8 命名 → shell/package.json bump **0.1.9**（commit c5dd8fd）
- 重跑 prepare-runtime：`.runtime-version = 0.1.9-0.1.1-rc.2-489c8bdf`（指纹与 0.1.8 版一致——依赖/vendor 未变，仅 ssid 版本号变化），冒烟全绿后替换安装目录归档（当前版备份 `.bak-0822b`）
- `npm run pack`：after-pack 确认 dsh-runtime.tar.gz 163.5MB 入包 → **思灵 Setup 0.1.9.exe（253.9MB）** 签名 + blockmap ✓
- tag v0.1.9 推送 + GitHub Release：https://github.com/Max-Null/seek-soul-in-darkness/releases/tag/v0.1.9（资产 Setup.0.1.9.exe / .blockmap，命名与 v0.1.6 一致；release-notes-v0.1.9.md 注明 v0.1.7/0.1.8 为本地构建未发）
- 教训：`gh release create` 的 `--assets` 在 PowerShell 下必须单字符串逗号分隔（数组会展开成多个参数导致解析失败）；`gh release edit` 后再 `upload` 单独传资产更稳。

### 5. 待办（用户操作）

- [ ] `npm run pack`（electron-builder NSIS，含标题栏海洋皮肤按钮等 asar 改动）
- [ ] GitHub Release（release-notes + 版本）
