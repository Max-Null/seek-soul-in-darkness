node_modules/
dist/
out/
build/
# 例外：shell/build/ 是 NSIS 定制脚本（installer.nsh），必须入库随包构建
!shell/build/
!shell/build/**
.DS_Store
*.log
.dsh/

dist-electron/
dist-electron-016/
*.tmp_probe/

# 内置运行环境（构建时由 scripts/prepare-runtime.mjs 生成，打包时进安装包）
shell/dsh-runtime/

kernel.bundle.mjs
exe-stderr.log
exe-stdout.log
shell/dsh-runtime.tar.gz